# drafts

Temporary Compound Lite HTML decision editors.

Skills that support `~html` write model JSON and editable HTML files here before
a final Markdown artifact exists. These files are for human judgment, selection,
and editing.

After using an editor, export Markdown from the browser and place the exported
file in the appropriate durable artifact directory.

Generated `.html` and `.json` files in this directory are ignored by default.
