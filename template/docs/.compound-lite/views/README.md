# views

Read-only Compound Lite HTML views.

`cl-render` writes browser-friendly views of finalized Markdown artifacts here.
The source Markdown file remains the artifact of record.

Generated `.html` files in this directory are ignored by default.
